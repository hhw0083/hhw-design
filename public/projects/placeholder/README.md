# Placeholder Project Images

Use this folder only for shared temporary portfolio placeholders. Project-
specific final assets should live in their own folder under `public/projects`.

Register placeholder paths in `src/data/projects.ts`; do not hardcode them in
UI components.
